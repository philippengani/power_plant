name: MLflow_Project

conda_env:conda.yaml

entry_points:
  main:
    parameters:
      maxDepths: {type: int, default: 3}
      numTrees: {type: int, default: 10}
    command: "python train_sk.py {maxDepths} {numTrees}"
